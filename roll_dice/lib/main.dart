import 'package:flutter/material.dart'; // Main package for flutter, which can use 

//pubspec.yaml contains all our dependencies which is later used to 


void main(){ // main() is where the main part of the code is run.
  runApp();// runApp takes in the parameter which is used to run our app.
}